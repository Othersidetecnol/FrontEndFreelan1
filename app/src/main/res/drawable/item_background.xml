<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#D3D3D3" /> <!-- Cor mais escura ao clicar -->
            <corners android:radius="8dp" /> <!-- Bordas arredondadas -->
            <stroke android:width="2dp" android:color="#000000" /> <!-- Cor da borda -->
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Cor padrão -->
            <corners android:radius="8dp" /> <!-- Bordas arredondadas -->
            <stroke android:width="2dp" android:color="#000000" /> <!-- Cor da borda -->
        </shape>
    </item>
</selector>
